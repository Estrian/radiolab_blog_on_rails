<% @posts.each do |post| %>
  <h2><%= post[:title] %></h2>
  <p>By: <%= post[:author][:first_name] %> <%= post[:author][:last_name] %></p>
  <p><%= post[:body] %></p>
  <p>Tags: <%= post[:tags].join(', ') %></p>
<% end %>
